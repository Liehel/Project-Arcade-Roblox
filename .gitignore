# Project place file
/Roblox.rbxlx

# Roblox Studio lock files
/*.rbxlx.lock
/*.rbxl.lock

sourcemap.json

# Carpetas de trabajo de sesiones de depuracion (no son fuente del proyecto).
# _versions se extrae del propio historial de git: commitearla seria duplicar
# 25 MB que ya estan dentro del repo.
/_versions/
/_session_backup/
